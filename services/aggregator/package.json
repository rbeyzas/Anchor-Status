{
  "name": "@anchor-oracle/aggregator",
  "version": "0.1.0",
  "private": true,
  "description": "Merges RealMainnet/RealTestnet/SimulatedMock reports and submits them to PerformanceOracle on testnet.",
  "type": "module",
  "scripts": {
    "aggregate": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^17.0.1",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^5.0.0"
  }
}
